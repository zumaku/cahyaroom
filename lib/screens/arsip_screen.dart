import 'package:flutter/material.dart';

class ArsipScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Arsip Screen'),
    );
  }
}
